name: jagdschule
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `pub publish`. This is preferred for private packages.
publish_to: 'none' 

version: 0.0.1+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  provider: ^5.0.0  
  path_provider: ^2.0.1
  #  csv: ^4.1.0            add again as soon as 5.0.0 is released 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:    
    sdk: flutter     
  test: ^1.14.4      
  flutter_launcher_icons: ^0.8.0

flutter_icons:
  android: true
  ios: true
  image_path: "assets/logoFilled.png"



flutter:
  uses-material-design: true

  assets:
    - assets/images.csv
    - assets/info.txt
